[pytest]
DJANGO_SETTINGS_MODULE = student_moving_marketplace.settings
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --strict-markers
    --tb=short
    --maxfail=5
testpaths = tests
